# Secret — broker credentials live here
shell/config.js

# Bundled binary, already present on the kiosk; re-download if needed
shell/mqtt.min.js

# Local scratch
*.log

input/
.env

# Local photo assets; supply your own on the kiosk. The whole directory is
# ignored, not just image.jpg — naming one file meant a second asset dropped in
# later would have been committed silently.
shell/media/

# Local agent tooling — commands, skills and machine-local settings. Personal
# workflow, not panel code, and settings.local.json carries paths from other
# projects. Kept out of a public repo.
.claude/
